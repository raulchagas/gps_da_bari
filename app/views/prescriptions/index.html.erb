 <div class="container" style="margin-top: 80px">
  <%= link_to "add new prescription", new_prescription_path %>
  <div class="container d-flex justify-content-center" style="margin-top: 80px">
      <ul>
        <%@prescriptions.each do |prescription|%>
          <div class="card card-prescription-style d-flex flex-column align-items-center mb-2">
            <%= prescription.title%>
            <span>Usuário <%= prescription.user.email%></span>
            <%= link_to "see details", prescription_path(prescription) %>
              <%= link_to "Delete",
                prescription_path(prescription),
                data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, :class => "d-flex flex-column align-items-center btn-dark-green"
              %>
          </div>
        <% end %>
      </ul>
  </div>
</div>
